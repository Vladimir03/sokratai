{
  "servers": {
    "github": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-github"
      ]
    },
    "codex": {
      "command": "codex",
      "args": [
        "mcp-server",
        "-c",
        "sandbox_mode=\"read-only\"",
        "-c",
        "mcp_servers={}"
      ]
    }
  }
}
