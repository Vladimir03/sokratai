# Промпт для ревью раунда 3 — ChatGPT-5.6 (верификация fix-pass + новое)

Скоуп: диффы раундов 2–3 (коммиты `88e55af` + текущий). Первый раунд ревью уже
был (REQUEST CHANGES); это ревью проверяет исправления и новый код.

---

## Промпт (копировать целиком)

```
Ты — тот же senior-ревьюер, что дал REQUEST CHANGES по онлайн-доске SokratAI
(перформанс + UX). Автор прислал два раунда правок. Твоя задача — ВЕРИФИКАЦИЯ:
не искать новые поверхности, а проверить, что заявленные исправления реальны,
не внесли регрессий, и оценить новый код теми же мерками.

Контекст продукта прежний: доска для живого урока репетитора; планшет со
стилусом; страница к концу урока — сотни штрихов; baseline Safari 15 / iOS 15;
нестабильная RU-сеть; главный Job — «закрыл урок → PDF сам в материалах занятия».

## Что автор утверждает — проверь каждое утверждение по коду

Сохранение (было P0 «гонка может записать старое поверх нового»):
1. Автосейв вынесен в класс AutosaveQueue (src/lib/whiteboard/autosaveQueue.ts):
   single-flight (параллельный flush получает тот же promise), цикл дренит
   очередь до конца, ошибка возвращает id в очередь и авторетраит, 'saved'
   только при пустой очереди. Есть юнит-тесты (autosaveQueue.test.ts).
   ПРОВЕРЬ: остались ли пути записи мимо очереди; может ли завершившийся цикл
   «съесть» правку, пришедшую между чтением pagesRef и ответом сервера; честен
   ли статус во ВСЕХ ветках (включая dispose и unmount).
2. beforeunload при несохранённом; выход кнопкой «Доски» ждёт flush, при
   неудаче — confirm. ПРОВЕРЬ обход: браузерный Back (history.back) редактор
   не перехватывает — оцени, критично ли это при текущем beforeunload.

Главный Job (было P0 «автоприкрепление — только кнопка»):
3. Выход с доски занятия авто-собирает PDF и прикрепляет (handleExit →
   attachPdfToLesson), идемпотентно по подписи (elements+background+gridMm+
   orientation). ПРОВЕРЬ: какие пути выхода НЕ покрыты; что при падении
   прикрепления; двойной вызов.

Ввод (было P1 «hot paths O(n) на каждое событие»):
4. Живой ввод — ref + rAF (макс. один setState за кадр); rect кэшируется на
   pointerdown; перетаскивание — SVG transform с одним коммитом на pointerup;
   ластик — bbox-отсев через WeakMap-кэш; SVG-узлы элементов кэшируются
   WeakMap (elementToSvg). ПРОВЕРЬ: инвалидируются ли кэши корректно
   (иммутабельность элементов — bumpVersion создаёт новый объект); нет ли
   утечки rAF; корректен ли multi-pointer (drag привязан к pointerId).

Текст (баг владельца, чинился ТРИЖДЫ — посмотри придирчиво):
5. Причина: default-действие mousedown уводило фокус на body → autoFocus-textarea
   ловил мгновенный blur → закрывался пустым. Фикс: event.preventDefault() на
   pointerdown холста + явный blur() активного редактируемого поля + id-гард в
   commitTextDraft. ПРОВЕРЬ цепочку на десктопе И на iOS (виртуальная
   клавиатура, программный focus внутри жеста); что теперь с blur'ом заголовка
   при рисовании; не сломал ли preventDefault что-то ещё (drag-scroll, клики).

Новое в раунде 3 — ревьюй как новый код:
6. Ориентация листа per-page: хранится в board_pages.app_state.orientation
   (jsonb, без миграции), pageSizeMm(orientation) вместо глобальных констант,
   экспорт делает смешанные документы (jsPDF addPage с ориентацией листа).
   ПРОВЕРЬ: все ли потребители геометрии переведены на размер-как-данные
   (canvas viewBox, clamp, миниатюры, textarea-позиционирование, PDF);
   предупреждение при контенте за краем нового листа.
7. Зум: 'fit' | 50–300% (процент ширины вьюпорта), ResizeObserver на скролл-
   контейнере. ПРОВЕРЬ: поведение при ресайзе окна/повороте планшета, при
   переключении страниц разной ориентации, деление на ноль/NaN при нулевом
   вьюпорте, и главное — координаты указателя при zoom>fit (rect кэшируется на
   pointerdown: не устаревает ли он при скролле контейнера ВО ВРЕМЯ жеста?).
8. SceneHistory (sceneHistory.ts): дедуп по ссылке, лимит 60, redo-инвалидация.
   ПРОВЕРЬ сценарии: undo после смены страницы, undo после удаления страницы,
   память на длинном уроке.

## Файлы

src/pages/tutor/Whiteboard.tsx · src/components/whiteboard/{BoardCanvas,
BoardToolbar,PagesPanel}.tsx · src/lib/whiteboard/{model,svg,exportPdf,
autosaveQueue,sceneHistory}.ts (+ *.test.ts)

## Формат ответа

По каждому пункту 1–8: ПОДТВЕРЖДЕНО / НЕ ПОДТВЕРЖДЕНО (с местом в коде) /
ПОДТВЕРЖДЕНО С ОГОВОРКОЙ. Затем новые находки [P0–P3] в прежнем формате
(файл, сценарий, причина, минимальный фикс) — только реальные, без добора.
В конце — вердикт APPROVED / REQUEST CHANGES и что обязательно до пилота.
```
