# Онлайн-доска — задачи реализации

Спека: [spec.md](./spec.md). Промпт одним заходом: [prompts.md](./prompts.md).

**Приёмочный критерий всего MVP:** репетитор провёл урок, ничего специально не нажимал, PDF оказался в материалах занятия, ученик его открыл. Пока это не работает — к косметике не переходить.

---

## Фаза 0 — гейт совместимости ✅ ВЫПОЛНЕН 2026-07-29 → **NO-GO, идём планом Б**

- [x] **W0.1** `npm i @excalidraw/excalidraw mathlive` → `npm run build` → `npm run smoke-check` — **smoke-check §1 красный** (lookbehind в `dist/assets`)
- [x] **W0.2** Греп `dist/assets`: незащищённый lookbehind в mermaid-хвосте excalidraw (**ломает Safari/iOS ≤ 16.3** — класс инцидента Глеба, rule 80); в ядре excalidraw lookbehind за feature-detect'ом; плюс `structuredClone` / `Array.at` / `Object.hasOwn` / `crypto.randomUUID` (Safari < 15.4) в новых чанках. mathlive: lookbehind нет, но `.at(` есть
- [x] **W0.3** Прирост: **+7,79 МБ raw** (7,59 → 15,38 МБ, +133 файла; gzip-хвост ≈ 2,3+ МБ), крупнейшие чанки — mermaid/cytoscape/шрифты, доске не нужные. Бонус-находка: первая сборка уронила esbuild-сервис → риск OOM-деплоев на VPS (1 ГБ RAM)
- [ ] ~~**W0.4** планшет со стилусом~~ — не выполнялся: NO-GO наступил раньше. Для плана Б лаг/нажим проверяются на своём холсте (W1.0.5)

**Итог: план Б** — `perfect-freehand` + свой canvas (MIT, ~5 КБ, тот же алгоритм штриха, что внутри Excalidraw). Пакеты откачены, `package.json` чист. Детали и цифры — spec §3 Р4.
**mathlive остаётся** (лицензия и lookbehind чистые), но только с полифиллами `Array.prototype.at` / `Object.hasOwn` / `structuredClone` — остаточный гейт на W2.4 (spec §7).

---

## Фаза 1 — сквозная цепочка (приёмочная)

Цель: Job Елены закрыт целиком, пусть на минимуме инструментов.

### Данные и бэкенд
- [x] **W1.1** Миграция `20260729120000_whiteboard.sql`: `boards`, `board_pages`, `board_sessions`. RLS tutor-only через SECURITY DEFINER `board_owned_by_tutor` (прямой подзапрос к `boards` дал бы false-negative — RLS на промежуточной таблице, rule 100). Студенческих политик в Фазе 1 НЕТ намеренно: доска однопользовательская, ученик получает ценность через PDF в материалах
- [x] **W1.2** Edge `whiteboard-api` (`verify_jwt=true`), 9 роутов, плоский `{error, code}` по-русски (rule 97), FK-конверсия `resolveTutorPkId` (rule 60)
- [x] ⚠️ **W1.3** Задеплоено агентом Lovable 2026-07-29. Проверено независимо: `OPTIONS .../whiteboard-api` → **204** (жива, не 404/503); таблицы в проде отвечают **42501 permission denied** для anon (существуют и закрыты — `PGRST205` означал бы, что миграции нет).
  - ⚠️ **`verify_jwt` фактически FALSE**, вопреки `config.toml`. Отличено по ТЕЛУ ответа (rule 96 §12): запрос без токена возвращает нашу русскую фразу `{"error":"Нет активной сессии.","code":"UNAUTHORIZED"}`, а не отказ шлюза. Данные защищены собственным гардом функции (GoTrue-проверка токена → `resolveTutorPkId` → 403), но слой шлюза отсутствует. Платформенное поведение, из проекта не управляется — см. memory `project_verify_jwt_declared_vs_live_2026_07_27`.
  - ⚠️ Агент добавил `GRANT ALL ... TO service_role` — без него edge (единственный write-path) падал бы с permission denied. В исходном файле этого не было: предполагалось, что хватит default privileges схемы. **Не хватает — для новых public-таблиц грант нужен явно.**
  - ⚠️ **В репозитории теперь ДВА файла, создающих таблицы доски:** мой `20260729120000_whiteboard.sql` и копия, которую Lovable закоммитил под своим именем — `20260728231726_a82b8e9c-4f35-485e-b4e2-501fd9147970.sql` (то же содержимое + гранты). Обе идемпотентны (`CREATE TABLE IF NOT EXISTS`, `DROP POLICY IF EXISTS`, `CREATE OR REPLACE`), и порядок по времени безопасен: копия Lovable раньше, моя доливается вхолостую и грантов service_role не отзывает. Удалять ничего не нужно, но **при следующей правке схемы доски править надо ту, что применяется, и помнить про дубль**.

### Ядро холста (план Б — пишем сами)
- [x] **W1.0.1** `src/lib/whiteboard/model.ts`: элементы stroke/shape/text, координаты в **миллиметрах** (страница = A4 минус поля ⇒ масштаб печати выводится сам), `version`/`versionNonce`, fail-soft `parseElements`. Покрыто `model.test.ts` (18 тестов)
- [x] **W1.0.2** `src/lib/whiteboard/svg.ts` — ОДИН источник геометрии: экран рендерит React-ом из тех же `SvgNodeSpec`, из которых экспорт собирает строку (spec §5 — расходиться им нельзя)
- [x] **W1.0.3** Штрих: pointer events → `perfect-freehand` → SVG-path заливкой контура; живой штрих в отдельном слое, не в общем массиве
- [x] **W1.0.4** Инструменты: перо, ластик (hit-test по ломаной, не по bbox), текст, линия/прямоугольник/эллипс, выделение рамкой + перемещение, undo/redo (Ctrl+Z / Ctrl+Shift+Z)
- [ ] **W1.0.5** Прогон на реальном планшете со стилусом: лаг, нажим (`simulatePressure`/`thinning` у perfect-freehand), palm rejection. **Нажим уже гейтится по `pointerType === 'pen'`** — мышь и палец шлют константу 0.5, и доверять ей нельзя

### Холст и страницы
- [x] **W1.4** Страница = отдельная сцена: свой массив `elements`, своя разлиновка, своя история undo/redo
- [x] **W1.5** `PagesPanel`: миниатюры (разлиновку в них не рисуем — в масштабе 1:10 она превращается в серую заливку), `+`, удаление, переключение
- [x] **W1.6** Разлиновка на уровне страницы: чистый / клетка / линейка / точка, шаг 5 и 10 мм. Точки — ОДИН `<path>` из вырожденных сегментов с круглым концом, а не тысяча `<circle>`
- [x] **W1.7** Автосохранение с дебаунсом 900 мс + досохранение при уходе со страницы; сбой возвращает страницу в очередь, а не теряет её молча

### Экспорт и главное
- [x] **W1.8** Пайплайн PDF: свой SVG → `svg2pdf.js` → `jsPDF`, страница = лист. ⚠️ В документ встраивается Golos Text из `src/fonts` — стандартные шрифты jsPDF кодируются WinAnsi и кириллицы НЕ содержат
- [x] **W1.9** Масштаб печати из мм-координат: контент 180 мм ⇒ при клетке 5 мм ровно 36 клеток по ширине A4 (тест в `model.test.ts`)
- [x] 🔴 **W1.10** Прикрепление PDF к занятию существующим путём `uploadLessonPdf` → `tutor_lesson_materials` (`kind:'pdf'`) — новой сущности нет, ученик видит конспект без единой правки на своей стороне. Прошлый автоэкспорт удаляется, чтобы не съесть кап в 5 PDF
- [x] **W1.11** Кнопка «Доска» в карточке занятия (индивидуального и группового) + **пункт «Доска» в левом меню** и список досок `/tutor/board`. В `TutorSchedule.tsx` — только `navigate` на маршрут-резолвер, без async-логики
- [x] **W1.12** Доска занятия открывается повторно: `GET /lessons/:id/board` идемпотентен (найти-или-создать)

**Проверка фазы:** приёмочный сценарий из spec §7, пункты 1, 3, 7, 8, 9.

---

## Ревью Фазы 1 (ChatGPT-5.6, 2026-07-29) — REQUEST CHANGES → fix-pass выполнен

Исправлено (раунд 2):
- **P0 гонка автосейва** → single-flight `flushDirty` (второй вызов получает тот же promise), очередь дренится до конца, при ошибке страницы возвращаются в очередь + авторетрай 5с; статус честный: «Сохранено» только при пустой очереди, ошибка — красная кликабельная кнопка «Повторить», `aria-live`, виден и на телефоне.
- **P0 уход без сохранения** → `beforeunload`-гард при несохранённом; выход кнопкой «Доски» ждёт flush, при неудаче — явный confirm «выйти без изменений?».
- **P0 авто-прикрепление** → выход с доски занятия автоматически собирает PDF и прикрепляет (идемпотентно по подписи, теперь включающей background/gridMm) с оверлеем «Прикрепляем конспект…»; ошибка прикрепления не запирает пользователя. Серверный finalizer «на закрытие вкладки» — Фаза 1.5.
- **P0 удаление страницы** → `window.confirm` с номером страницы и числом объектов; кнопка доступна с клавиатуры (`group-focus-within`).
- **P1 hot paths** → живой ввод копится в ref и уходит в state раз за кадр (rAF); rect кэшируется на pointerdown; перетаскивание — SVG `transform` с ОДНИМ коммитом на pointerup (заодно чинит undo движения); hit-test ластика — быстрый отсев по WeakMap-кэшу bbox; SVG-узлы элементов кэшируются (миниатюры больше не гоняют perfect-freehand по всей странице).
- **P1 бандл** → PDF-движок динамическим импортом: чанк доски 537 → **37 КБ** (gzip 14), `exportPdf` 505 КБ грузится только на экспорте; прогресс по листам на кнопке, yield между страницами, шрифт fail-closed (без молчаливых «????» вместо кириллицы).
- **P1 multi-pointer** → drag привязан к `pointerId`, ладонь/второй палец не обрывают штрих.
- **P1 undo** → Ctrl+Z игнорирует input/textarea/contenteditable; движение отменяется одним шагом.
- **P1 мобильный** → редактор full-bleed (`fixed inset-0` поверх AppFrame), тач-цели 44px, `touch-pan-x` на ленте страниц.
- **P2** → имена цветов словами для скринридера, aria-label холста, ephemeral live-элементы без модельных фабрик, зеркала ref синхронизируются в эффектах.

Баги ручного теста владельца (все три воспроизведены и починены):
1. **Название не сохранялось** — onBlur сравнивал с `board.title`, который onChange уже обновил → «изменений нет». Теперь сравнение с последним СОХРАНЁННЫМ (`savedTitleRef`), Enter коммитит.
2. **Доска из занятия не создавалась** — FK-дрейф (rule 60): в `boards.student_id` (→ `tutor_students.id`) вставлялся `tutor_lessons.student_id` (это `profiles.id`!) → FK violation → 500. Нужное поле — `tutor_lessons.tutor_student_id`. ⚠️ Требует редеплоя edge.
3. **Текст не вставлялся** — гонка blur ↔ pointerdown: клик по холсту открывал новый черновик, затем blur старого стирал оба. Коммит теперь через ref с проверкой id черновика; Enter — готово, Shift+Enter — новая строка.

Принято сознательно (P2–P3, отложено): ленивая загрузка страниц по переключению; IndexedDB-снапшот для восстановления; серверный finalizer; spatial buckets для hit-test (bbox-кэша пока достаточно); клавиатурное перемещение элементов; идемпотентность attach при потерянном ответе. «Чего не хватает» из ревью (вставка изображений, zoom/pan) = W2.1–W2.2 Фазы 2 — ревью подтвердило их приоритет.

## Раунд 3 (2026-07-29): текст — настоящая причина, площадь листа, архитектура

- **Текст (третий заход, корень найден).** Textarea монтировался и фокусировался ВНУТРИ pointerdown, после чего браузер выполнял default-действие mousedown — уводил фокус на body → мгновенный blur → blur-коммит пустого значения закрывал поле в ту же миллисекунду. Фикс: `event.preventDefault()` на pointerdown холста (отменённый pointerdown подавляет compat-mousedown → фокус никто не крадёт) + детерминированный `blur()` активного редактируемого поля перед новым жестом. Прошлый id-гард остаётся — он закрывает вторую гонку (blur ↔ новый черновик).
- **Площадь листа** (запрос владельца): per-page **ориентация** — горизонтальный A4 даёт 267 мм ширины вместо 180 (+48%); хранится в `board_pages.app_state.orientation` (jsonb, БЕЗ миграции), экспорт собирает смешанные документы (лист = своя ориентация). Плюс **зум**: «Вписать» / по ширине (100%) / 50–300% шагом 25 — fit-width на ноутбуке сам по себе сильно увеличивает рабочую площадь. При смене ориентации с контентом за краем — предупреждение (контент не удаляется, но в PDF не попадёт). Многолистовые «высокие» страницы (N×A4 с нарезкой при экспорте) — сознательно отложено.
- **Архитектура:** сетевое сохранение вынесено в чистый класс `AutosaveQueue` (single-flight, дренаж до конца, возврат в очередь при ошибке, авторетрай — 7 юнит-тестов с fake timers), undo/redo — в `SceneHistory` (дедуп burst'ов, лимит, инвалидация redo — 5 тестов). `Whiteboard.tsx` стал оркестратором; геометрия листа — данные (`pageSizeMm(orientation)`), а не глобальные константы.

Итог: vitest 84/84 (+14), чанк доски 43 КБ (PDF-движок по-прежнему отдельно), smoke 24/24.

## Раунд 4 (2026-07-29): верификационное ревью → REQUEST CHANGES → fix-pass

Исправлено:
- **P0 stale-snapshot автосейва** → зеркало `pagesRef` стало СИНХРОННЫМ (`applyPages`: ref+state в одном тике, все мутации через него); очередь при любом раскладе читает новую сцену. + тест «страница, изменённая во время своего же сейва, сохраняется повторно»; `forget()` теперь возвращает статус к saved при пустой очереди (+тест).
- **P0 браузерный Back** → popstate-трап: сторожевая history-запись; при несохранённом или непривязанном PDF Back возвращает запись и ведёт через штатный `handleExit` (flush → attach → навигация); когда терять нечего — отпускает. `useBlocker` недоступен (BrowserRouter без data-роутера).
- **P0 молчаливый уход при ошибке attach** → confirm «Выйти БЕЗ PDF? / Остаться и повторить».
- **P1 attach single-flight** → общий `attachPromiseRef` для кнопки и выхода + кнопка «Доски» заблокирована во время attach.
- **P1 hot path** → статичная сцена вынесена в memo `SceneLayer` (при рисовании/перетаскивании reconcile сотен `<g>` не происходит); ластик копит попадания в ref, скрывает per-frame и коммитит ОДНИМ вызовом на pointerup — заодно один шаг undo на жест (**P1 про распад ластика на много Undo закрыт этим же**).
- **P1 pan на планшете** → второй палец во время touch-жеста переключает в панорамирование (штрих отбрасывается, накопленный ластик коммитится), `onPan` скроллит контейнер; кэш rect инвалидируется capture-слушателем scroll (штрих не уезжает при прокрутке во время жеста).
- SceneHistory: комментарий о памяти исправлен (O(limit × элементов), не O(изменённых)).

Принято сознательно / отклонено с аргументом:
- **Кросс-сессионная идемпотентность attach** — отклонено как P1: это оптимизация, не корректность. Повторный экспорт при переоткрытии ЗАМЕЩАЕТ материал (upload → update `export_material_id` → delete старого), дубликатов и утечки капа нет; честное решение — колонка `boards.export_signature` (миграция + edge) — в backlog Фазы 1.5.
- **Spatial grid для ластика** — отклонено: 800 bbox-проверок из WeakMap-кэша — микросекунды; реальная цена была в перерендере родителя на каждое попадание, это закрыто батчингом. Grid добавим, если планшетный прогон покажет обратное.
- Unit-тест mixed-orientation jsPDF — отложено: `doc.svg()` требует DOM, в node-окружении vitest не собирается; проверяется руками при E2E.

Итог: vitest 86/86, build ok, smoke 24/24. **Остаётся обязательным прогон на физическом iPad/iPhone (текст+клавиатура, стилус, Back-свайп, 800 штрихов × 10 страниц).**

## Фаза P0 «групповое занятие» (план `~/.claude/plans/shiny-leaping-stardust.md`, утверждён 29.07)

Решения владельца: **Р1 ОТМЕНЁН** → бесконечный холст + рамки-страницы (B0); транспорт v1 — снапшоты ~1–2 с (WS-релей = Фаза 2.5); гость без регистрации — в скоупе.

### Этап 1 ✅ (2026-07-29) — холст + камера + B3
- [x] **B0-фронт**: камера `{cx,cy,zoom}`, viewBox-окно, рамка = строка `board_pages` (позиция в `app_state.frame`, элементы ЛОКАЛЬНЫ рамке — `exportPdf` не менялся, миграции данных нет; старые доски ложатся в ряд). Culling: рамка вне окна не монтируется, элементы по кэшированным bbox, окно КВАНТОВАНО (шаг 100 мм — memo слоёв не сбрасывается на каждый кадр пана), LOD < 0.5 px/мм. Жесты: пусто/средняя кнопка = пан, два пальца = pan+pinch, колесо = плавный зум к курсору (закрыл B19), Shift+колесо = гор. пан.
- [x] **B3**: внутренний clipboard (`boardClipboard.ts`) — Ctrl+C копирует выделение, Ctrl+V сначала внутренний буфер (маркер в ОС-буфере отличает «наше» от свежего скриншота), потом ОС-картинки; вставка в центр вьюпорта, новые id/seq (`reassignIdentity`).
- [x] Квик-фиксы: выделение КОНТУРОМ вместо opacity 0.7 (корень B14); вставка картинок в рамку под центром вьюпорта (позиция из B10); «Вся доска» = fit-all (заготовка grid-обзора Елены).
- [x] Тесты: +11 (камера мир↔экран взаимно-обратны, zoom-к-точке неподвижен, fit-contain в разных окнах — основа follow B1, раскладка рамок, clipboard). Итого 104.
### Этап 2 ✅ код готов (2026-07-29/30) — сетка Chattern + рулоны + PDF-импорт + сервер
Решения владельца (вечер 29.07, по скриншотам Chattern и переписке с Еленой): сетка A4 вместо свободной раскладки; PDF-импорт в этот этап; схема «полоса + рулоны». Челлендж Елены принят частично: подписи человеческие (не `[0:0]`), порядок PDF из структуры (полоса → рулоны), «половинки страниц» не копируем, пустые листы пропускает только ЭКСПОРТ.
- [x] Сетка: `FrameCell {col,row}` в `app_state.frame` (legacy `{x,y}` Этапа 1 конвертируются к ближайшей ячейке); ландшафт = 2 колонки; ghost-ячейки-плюсы (справа полосы, внизу каждого рулона); подписи «Лист N» / «Маша · 2».
- [x] PDF-импорт: «Из PDF» → `renderPdfPagesToFiles` (динамический импорт, pdf.worker не в чанке доски) → лист-подложка на страницу в полосе, прогресс, кап 30 стр.
- [x] Экспорт: порядок из сетки (`compareCellsForExport`), пустые листы пропускаются; подпись attach включает ячейки (перестановка → новый PDF). ⚠️ Решение владельца 30.07: лист с PDF-подложкой БЕЗ пометок = **ЗАПОЛНЕННЫЙ** (подложка — элемент ⇒ `length>0` уже включает; фильтру запрещено «умнеть» до «есть ли что-то кроме подложки» — закреплено комментарием в `buildPdfBlob`).
- [x] Доревизия 30.07 перед коммитом: `tutor_students.name` → `display_name` в гостевых кандидатах `whiteboard-public` (селект несуществующей колонки молча отдавал бы ПУСТОЙ список учеников — гостевой вход был бы мёртв) и в подписях зон на фронте; + общий anti-brute троттл в `whiteboard-public` ДО резолва slug (перебор ссылок не ограничивался ничем).
- [x] Сервер: миграция `20260730120000_whiteboard_collab.sql` (zone_tutor_student_id, board_share_links + board_guests deny-all, board_page_revs + publication, `is_board_participant` с тройным REVOKE, participant-политики); `whiteboard-api` (+rev-bump fault-tolerant, `POST /boards/:id/zones` идемпотентная раздача рулонов fail-closed по составу, share create/revoke, zone в patch); новые edge `whiteboard-student-api` (запись своей зоны + лист в свой рулон, base_rev/409 c серверными elements) и `whiteboard-public` (meta/join/state/signals/save, throttle, кап 30 гостей, bearer'ы не логируются). `config.toml` ×2.
- [x] UI: «Раздать зоны» в хедере доски занятия; имена зон подтягиваются по RLS репетитора.
- [x] Ревью-промпт этапов 1–2 с целями по полевым метрикам /admin — `review-prompt-phase-p0.md`.
- [x] ДЕПЛОЙ Этапа 2: миграция применена, probe OPTIONS ×3 → 204 (30.07).

### Этап 3 ✅ код готов (2026-07-30) — вход ученика/гостя + живой синк
- [x] **Reconcile** (`reconcile.ts`, 6 тестов): LWW по (version, versionNonce), при равных версиях детерминированный тай-брейк МЕНЬШИМ nonce (Excalidraw-модель — обе стороны сходятся к одному результату), щит активного редактирования. Известное ограничение v1: без tombstones удалённый одной стороной элемент может «воскреснуть» при конфликте — принято планом.
- [x] **Синк**: `boardRealtime.subscribeBoardRevs` (канон rule 100 — уникальный суффикс топика, gap-fill на SUBSCRIBED после обрыва, merge-не-invalidate). Репетитор и ученик — Realtime; гость — поллинг `/signals` 2.5 с (15 с при скрытой вкладке). 409-путь: reconcile → немедленный повтор с новым base_rev; второй конфликт подряд → очередь с backoff.
- [x] **base_rev у ВСЕХ писателей**: репетиторский `handleSavePage` тоже проверяет rev при записи elements (репетитор конкурирует с учеником в его зоне) и возвращает rev; клиенты ведут rev per-frame.
- [x] **Ученик**: роут `/student/board/:boardId`; чтение PostgREST под RLS; «моя зона»/подписи зон/signed URL картинок — edge `GET /boards/:id/me` (у ученика НЕТ RLS на tutor_students и storage-прав на board-images). ⚠️ Доревизией пойман anti-leak: /me замкнут на ПОЛНУЮ проверку участия (`isParticipantStudent` — зеркало is_board_participant), иначе любой ученик любого репетитора получал бы имена зон и подписанные URL чужой доски по board_id.
- [x] **Гость**: роут `/b/:slug` без AuthGuard (join-экран «я — Маша» / свободное имя = зритель), guest_token в localStorage per slug; `image_urls` в state-ответе (rewriteToProxy — rule 96). Общий вью `SharedBoardView` (транспорт-инъекция: ученик/гость), компактный тулбар, запись только в свою зону, «+ Лист» в свой рулон.
- [x] **«Пригласить»**: кнопка в хедере доски → ссылка + «Копировать» + «Отправить ученикам в чат» (существующий write-path `ensureChatConversation`+`sendChatMessageApi`; пуш штатным каскадом) + «Закрыть доступ» (отзыв: все гости отваливаются на следующем запросе). Известное ограничение v1: 5-мин троттлинг уведомлений чата НЕ обходится (правка чат-edge — чужой канон); если переписка шла только что, пуш схлопнется — ученик и так в чате.
- [x] E2E-смок: `/b/<фейковый slug>` на dev — join-экран отрисован, прод-edge ответил русской фразой (цепочка фронт→функция подтверждена вживую).
- [ ] ДЕПЛОЙ Этапа 3: редеплой ВСЕХ ТРЁХ функций (whiteboard-api: base_rev+rev; student-api: /me+anti-leak; public: image_urls) — миграция НЕ нужна; deploy-sokratai для фронта. Затем Этап 4: follow/bring.

## Фаза 2 — то, ради чего выберут именно вас

Решения владельца (2026-07-29): порядок 2а → 2б; синк v1 = снапшоты ~1–2 с (без новой инфры); вход ученика через аккаунт; картинки вставляют оба (ученик — с мелом). Детали — spec §8 «Решения владельца».

### Фаза 2а — картинки ✅ реализовано 2026-07-29 (ждёт бакет + деплой)

- [x] **W2.1.1** Bucket + политики ЗАДЕПЛОЕНЫ 2026-07-29 (агент Lovable; проверено независимо: authenticated-эндпоинт отдаёт «Object not found», контрольный несуществующий бакет — «Bucket not found»; политики подтверждены его запросом к `pg_policies`). ⚠️ **`file_size_limit` и `allowed_mime_types` бакета = NULL**: платформа Lovable блокирует ЛЮБЫЕ записи в `storage.buckets` (включая UPDATE миграцией — «SQL writes … rejected»), а их create-tool принимает только имя+public. Защита сейчас клиентская (компрессия ≤4 МБ + отсечка >10 МБ в `boardImages.ts`); серверные лимиты выставить вручную в дашборде Storage при случае — это НЕ блокер, но прямой PostgREST-загрузчик со своим токеном мог бы класть большие файлы в свою папку
- [x] **W2.1.2** `ImageElement` — `x,y,w,h,rotation,ref`; bounds повёрнутого прямоугольника по углам, hit-test обратным поворотом, fail-soft parse (не-storage ref отбрасывается)
- [x] **W2.1.3** Resize за 4 угловые ручки с **неподвижным противоположным углом в мировых координатах** (иначе повёрнутая картинка «плывёт») + **rotate-ручка** с магнитом к 0/90/180/270 (±5°) — запрос владельца: фото приходят боком. Live-превью через скрытие оригинала + эфемерную копию; ОДИН коммит на жест. Ручки — только для картинок (фигуры проще перерисовать; отдельной задачей при запросе)
- [x] **W2.1.4** `<image>` с signed URL (кэш TTL 1ч, обновление за 5 мин до истечения; host уже api.sokratai.ru — rewriteToProxy не нужен); битый ref → рамка-плейсхолдер; failed-ref'ы не ретраятся на каждый штрих
- [x] **W2.1.5** Ctrl+V (window-уровень, не крадёт вставку из текстовых полей) + drag&drop с подсветкой зоны + кнопка-пикер (`accept="image/*"` — iOS/Android сами предлагают камеру). Компрессия 1600px/4МБ в `uploadBoardImage` (не дважды)
- [x] **W2.1.6** PDF: data:-URL всех ref до сборки, fail-closed (как со шрифтом)
- [x] **W2.1.7** Лимит **5 картинок на лист** (решение владельца — вес PDF); кап исходника 10 МБ; русские фразы
- [x] **W2.2** (влился в W2.1.5)
- Попутно: **исправлен live-регресс р.3** — memo `ElementNode` сравнивал id+version и замораживал live-превью штриха после первого кадра; сравнение переведено на идентичность объекта (элементы иммутабельны)

### Фаза 2б — ученик на доске (после 2а)

- [ ] **W2.9** ~~WS-релей~~ → **снапшот-синк v1**: Realtime-канал по `board_pages` как сигнал → точечный refetch страницы → merge по `version`/`versionNonce` (только merge, НИКОГДА invalidate — rule 100); relay отложен в Фазу 2.5
- [ ] **W2.9.1** Student-RLS: SELECT-политики на `boards`/`board_pages` через `student_can_see_lesson(lesson_id)` (SECURITY DEFINER, rule 98) + student-роуты в edge (whitelist полей); write ученика — только через edge при «мел у него»
- [ ] **W2.9.2** Студенческий роут `/student/board/:id` (read-only до мела) + кнопка «Позвать к доске» у репетитора (ссылка в чат репетитор↔ученик существующим write-path `tsc_post_message` через edge — rule 100)
- [ ] **W2.10** Поле «у кого мел» в `board_sessions`; правки принимаются только от держателя (сервер — авторитет)
- [ ] **W2.11** Кнопки «Вызвать к доске» / «Забрать мел». **Репетитор забирает без спроса**
- [ ] **W2.12** Уход ученика → мел возвращается репетитору автоматически
- [ ] **W2.13** Индикация «сейчас пишет Маша»
- [ ] **W2.14** Гостевой вход без аккаунта — ОТЛОЖЕН (решение владельца: аккаунт сейчас, гость позже)
- [ ] **W2.3** **Задача из Банка знаний на доску** одним кликом. Того же не может ни одна чужая доска
- [ ] **W2.4** Формулы на MathLive: вставка, редактирование двойным кликом. ⚠️ Сначала остаточный гейт (spec §7): `npm i mathlive` + полифиллы `Array.prototype.at`/`Object.hasOwn`/`structuredClone` в `main.tsx` ДО ленивых чанков, тем же PR
- [ ] **W2.5** Механика Idroo: `/` дробь, `^` степень, `_` индекс, `Enter` новая строка
- [ ] **W2.6** Палитра символов с **LaTeX-командой в подсказке** — учит синтаксису попутно
- [ ] **W2.7** Выравнивание по знакам равенства + **частичная раскраска фрагмента** формулы
- [ ] **W2.8** ⚠️ Формула отдаёт свой SVG для экспорта + растровый фолбэк 2–3× DPI

### Передача мела
- [ ] **W2.9** WS-релей в Москве (не Supabase Realtime — см. spec §5)
- [ ] **W2.10** Поле «у кого мел» в `board_sessions`; правки принимаются только от держателя
- [ ] **W2.11** Кнопки «Вызвать к доске» / «Забрать мел». **Репетитор забирает без спроса**
- [ ] **W2.12** Уход ученика → мел возвращается репетитору автоматически
- [ ] **W2.13** Индикация «сейчас пишет Маша»
- [ ] **W2.14** Вход ученика по ссылке без отдельной регистрации — переиспользовать паттерн `tutor_groups.join_code`

---

## Фаза 3 — качество ввода

По этому репетиторы реально сравнивают доски: «почти как на бумаге».

- [ ] **W3.1** 🔶 **Объединение близких штрихов в один объект** с настраиваемой чувствительностью. У Meleto это отдельный алгоритм, и, вероятно, именно он даёт «не виснет» и «приятный дизайн». В своей модели (план Б) группировка проектируется нативно — преимущество против Excalidraw, где каждый штрих навсегда отдельный элемент
- [ ] **W3.2** Опция отключения pressure sensitivity для Apple Pencil — `simulatePressure`/`thinning` у perfect-freehand (у Excalidraw это открытый баг [#11322](https://github.com/excalidraw/excalidraw/issues/11322))
- [ ] **W3.3** Настройка агрессивности сглаживания (`smoothing`/`streamline` у perfect-freehand)
- [ ] **W3.4** Ластик по частям объекта
- [ ] **W3.5** Блокировка объекта (чтобы ученик не сдвинул условие задачи)
- [ ] **W3.6** Лазерная указка

---

## Фаза 4 — группы (после)

- [ ] **W4.1** Модель Whiteboard.fi: у каждого ученика своя доска, репетитор видит все миниатюрами
- [ ] **W4.2** Приватное вмешательство в доску одного ученика
- [ ] **W4.3** Рассылка задания на доски всех учеников одним кликом

---

## Сквозные инварианты

- ⚠️ **Ink не через Supabase Realtime** — лимит 500 сообщений/сек на весь проект, одна пара даёт ~100/сек; плюс US-регион = 250–400 мс
- ⚠️ **`tutor_lessons` не трогать** — high-risk (rule 10), только новые таблицы
- ⚠️ **FK-дрейф:** `tutor_students.tutor_id → tutors.id`, `homework_tutor_assignments.tutor_id → auth.users.id` (rule 60)
- ⚠️ **Safari 15 baseline** (rule 80): `parseISO` вместо `new Date("…")`, `100dvh` вместо `100vh`, 16px на инпутах
- ⚠️ **Миграции и edge-функции НЕ автодеплоятся** — отдельная просьба агенту Lovable + проверка `curl -X OPTIONS`
- ⚠️ **Фронт не доезжает до прода сам** — после правок в `src/**` нужен `deploy-sokratai` (rule 95)
- 🔴 **tldraw использовать нельзя** — проприетарная лицензия, прод запрещён без ключа (~$6 000/год)
- 🔴 **Excalidraw использовать нельзя** — NO-GO гейта 2026-07-29: незащищённый lookbehind в mermaid-хвосте (Safari/iOS ≤ 16.3) + 7,79 МБ хвоста + красный smoke-check §1. Детали — spec §3 Р4
- ⚠️ **mathlive — только с полифиллами** `Array.prototype.at` / `Object.hasOwn` / `structuredClone` в `main.tsx` до ленивых чанков, тем же PR (spec §7)
