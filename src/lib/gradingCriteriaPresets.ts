import type { GradingCriterion } from '@/lib/tutorHomeworkApi';

/**
 * Готовые пресеты критериев оценки для конструктора ДЗ (criteria-grading, 2026-06).
 *
 * Кнопка «Загрузить критерии …» в `HWTaskCard` заполняет задачу одним из этих
 * шаблонов (название + макс. балл + опц. описание/зависимость). Frontend-зеркало
 * backend-пресетов (`supabase/functions/_shared/subject-rubrics/*-ege.ts`).
 *
 * ⚠ Точные max-баллы и band-описания валидирует репетитор-предметник (как Егор —
 * физику, Эмилия — DELF). Структуру (Σ, cascade) — из официальных критериев ФИПИ.
 */

const K1_LABEL = 'К1: Отражение позиции автора (рассказчика)';

/**
 * ЕГЭ русский язык — сочинение задание 27 (реформа 2024+). Σ = 22.
 * К1–К6 — содержание (AI оценивает уверенно); К7–К10 — грамотность (AI даёт
 * предварительный балл, репетитор перепроверяет). Cascade: К1=0 ⇒ К2,К3=0.
 */
//
// ⚠ МИРРОР: `label` / `max` / `depends_on_zero` / `description` — byte-for-byte =
// backend `supabase/functions/_shared/subject-rubrics/russian-ege.ts::RUSSIAN_EGE_27_CRITERIA`.
// `description` (band-гайд) ОБЯЗАТЕЛЕН: кнопка пресета пишет эти критерии в
// `grading_criteria_json` → они приходят в resolver как tutor grading_criteria
// (высший приоритет, перекрывают backend-пресет) → в покритериальный блок промпта
// попадает ИМЕННО эта версия. Без `description` AI грейдил бы по label+max
// (review fix #4, 2026-06-29). Меняешь band тут — синхронно правь backend.
export const RUSSIAN_EGE_27_PRESET: GradingCriterion[] = [
  {
    label: K1_LABEL,
    max: 1,
    description:
      '1 — позиция автора (рассказчика) по указанной проблеме сформулирована верно (в любой части сочинения). 0 — не сформулирована или сформулирована неверно.',
  },
  {
    label: 'К2: Комментарий к позиции автора',
    max: 3,
    depends_on_zero: [K1_LABEL],
    description:
      '3 — приведено 2 примера-иллюстрации из прочитанного текста, важных для понимания позиции автора, с пояснением к КАЖДОМУ + указана и пояснена смысловая связь между ними. 2 — 2 примера с пояснениями, но смысловая связь не указана / указана неверно / без пояснения. 1 — 1 пример с пояснением. 0 — примеров нет / пример без пояснения / вместо комментария пересказ / процитирован большой фрагмент / без опоры на текст. Пример-иллюстрация без пояснения НЕ засчитывается.',
  },
  {
    label: 'К3: Собственное отношение экзаменуемого',
    max: 2,
    depends_on_zero: [K1_LABEL],
    description:
      '2 — собственное отношение к позиции автора сформулировано и обосновано, приведён пример-аргумент (читательский / историко-культурный / жизненный опыт). 1 — обосновано, но пример-аргумент не приведён, ИЛИ пример-аргумент есть, но отношение не заявлено. 0 — отношение заявлено формально («согласен / не согласен») / не обосновано / не соответствует проблеме. Не учитываются примеры-аргументы из комиксов, аниме, манги, фанфиков, графических романов, компьютерных игр.',
  },
  {
    label: 'К4: Фактическая точность речи',
    max: 1,
    description:
      '1 — фактические ошибки отсутствуют. 0 — допущена 1 фактическая ошибка или более (в фоновом материале, в примерах, в опоре на исходный текст).',
  },
  {
    label: 'К5: Логичность речи',
    max: 2,
    description: '2 — логические ошибки отсутствуют. 1 — 1–2 логические ошибки. 0 — 3 логические ошибки или более.',
  },
  {
    label: 'К6: Соблюдение этических норм',
    max: 1,
    description:
      '1 — этические нормы соблюдены. 0 — есть высказывания, нарушающие этические нормы / законодательство РФ / нецензурная брань.',
  },
  {
    label: 'К7: Соблюдение орфографических норм',
    max: 3,
    description:
      'Подсчёт орфографических ошибок: 0 ошибок — 3; 1–2 — 2; 3–4 — 1; 5+ — 0. Предварительная оценка — репетитор перепроверит. Будь осторожен: если текст распознан с фото, считай балл ориентировочным.',
  },
  {
    label: 'К8: Соблюдение пунктуационных норм',
    max: 3,
    description:
      'Подсчёт пунктуационных ошибок: 0 ошибок — 3; 1–2 — 2; 3–4 — 1; 5+ — 0. Предварительная оценка — репетитор перепроверит.',
  },
  {
    label: 'К9: Соблюдение грамматических норм',
    max: 3,
    description:
      'Подсчёт грамматических ошибок (согласование, управление, формы слов): 0 ошибок — 3; 1–2 — 2; 3–4 — 1; 5+ — 0. Предварительная оценка — репетитор перепроверит.',
  },
  {
    label: 'К10: Соблюдение речевых норм',
    max: 3,
    description:
      'Подсчёт речевых ошибок (тавтология, неуместное слово, плеоназм): 0 ошибок — 3; 1–2 — 2; 3–4 — 1; 5+ — 0. Предварительная оценка — репетитор перепроверит.',
  },
];

export interface GradingCriteriaPreset {
  id: string;
  label: string;
  /** Авто-устанавливаемый max_score задачи (= Σ max критериев). */
  totalMax: number;
  criteria: GradingCriterion[];
}

/**
 * Реестр пресетов (forward-extensible). v1 — один пресет (ЕГЭ-русский).
 * Новый предмет/экзамен → добавь сюда + frontend-константу + backend `*-ege.ts`.
 */
export const GRADING_CRITERIA_PRESETS: GradingCriteriaPreset[] = [
  {
    id: 'russian-ege-27',
    label: 'ЕГЭ русский — сочинение (К1–К10)',
    totalMax: 22,
    criteria: RUSSIAN_EGE_27_PRESET,
  },
];

/** Σ max по всем критериям (человеко-читаемый полный итог, напр. ЕГЭ Σ=22). */
export function sumCriteriaMax(criteria: GradingCriterion[] | null | undefined): number {
  if (!Array.isArray(criteria)) return 0;
  return criteria.reduce((sum, c) => sum + (Number.isFinite(c.max) ? c.max : 0), 0);
}

/**
 * Σ max ТОЛЬКО по AI-оцениваемым критериям (исключает `tutor_only`). Зеркало
 * backend `sumAiGradableMax` (guided_ai.ts). Движок грейдинга требует
 * `max_score == aiGradableMax` (иначе `mapAiScoreToTemplateScale` делает
 * пропорциональный ремап и искажает покритериальный балл), поэтому авто-`max_score`
 * редактора и сверка Σ-бейджа считаются по ЭТОЙ сумме, а не по `sumCriteriaMax`.
 * Для русского пресета (все К1–К10 = `ai`) обе суммы равны 22 → поведение то же.
 */
export function sumAiGradableCriteriaMax(criteria: GradingCriterion[] | null | undefined): number {
  if (!Array.isArray(criteria)) return 0;
  return criteria
    .filter((c) => c.kind !== 'tutor_only')
    .reduce((sum, c) => sum + (Number.isFinite(c.max) ? c.max : 0), 0);
}
